// square.h

class Square {
  public:
      // MUTATORS
      static const double square( const double root );
        // Random unit test function A. Takes a const double and multiplies it by itself.
      static const double squareRoot( const double value );
        // Random unit test function B. Takes a const double and uses cmath's sqrt on it.
};